% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_1to3.R
\name{run_1to3}
\alias{run_1to3}
\title{Run Steps 1→2→3 (add sample → run MOFA → plot per-model PDFs)}
\usage{
run_1to3(
  test_matrix_path,
  mofa_dir = system.file("extdata", package = "MESOMICS"),
  value_data_types = c("D_exprB_MOFA"),
  inputs_dir = "output/",
  models_dir = "output/",
  mesomics_csv = system.file("extdata", "MESOMICS_latent_factors.csv", package =
    "MESOMICS"),
  group = "group1",
  python_bin,
  out_ternary_file = file.path(models_dir, "plots", "ternary_plot_all_samples.svg")
)
}
\arguments{
\item{test_matrix_path}{Character vector of CSV paths (first column = gene IDs).}

\item{mofa_dir}{Directory containing MOFA .RData inputs.}

\item{value_data_types}{MOFA object names to update; same length as `test_matrix_path`}

\item{inputs_dir}{Output dir for Step 1; also used as `inputs_dir` for Step 2 .}

\item{models_dir}{Output dir for Step 2; Step 3 reads from here.}

\item{mesomics_csv}{Path to MESOMICS_latent_factors.csv}

\item{group}{MOFA group for plotting (default "group1").}

\item{python_bin}{Python environment}
}
\value{
Invisibly, a list with:
\itemize{
  \item step1_files  — vector of .RData files in `inputs_dir`
  \item step2_status — named logical vector from run_mofa()
  \item step3_pdfs   — character vector of written PDF paths
}
}
\description{
Pipeline wrapper:
\enumerate{
  \item \code{add_sample_to_mofa()} — writes updated .RData to \code{inputs_dir}
  \item \code{run_mofa()} — writes \code{MOFA-<sample>.hdf5} to \code{models_dir}
  \item \code{plot_test_samples()} — writes per-model PDFs to \code{models_dir/plots}
}
}
